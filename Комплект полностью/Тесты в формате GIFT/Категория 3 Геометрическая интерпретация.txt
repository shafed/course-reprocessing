::К3_В1:: Каков геометрический смысл ядра оператора ортогональной проекции на плоскость $Oxy$ в $\mathbb{R}^3$? {
    =Ось $Oz$ (прямая, перпендикулярная плоскости проекции).
    =Плоскость $Oxy$.
    =Начало координат.
    =Вся пространство $\mathbb{R}^3$.
}

::К3_В2:: Оператор поворота на угол $\alpha$ в $\mathbb{R}^2$. При каком условии он необратим? {
    =Никогда (всегда обратим).
    =При $\alpha = 0$.
    =При $\alpha = \pi$.
    =При $\alpha = \pi/2$.
}

::К3_В3:: Рассмотрим оператор $\hat{A}\vec{x} = \vec{x} \times \vec{n}$ в $\mathbb{R}^3$, где $\vec{n} \neq \vec{0}$. Что является образом этого оператора? {
    =Плоскость, перпендикулярная вектору $\vec{n}$.
    =Прямая, коллинеарная вектору $\vec{n}$.
    =Все пространство $\mathbb{R}^3$.
    =Нулевое подпространство.
}

::К3_В4:: Чему равен ранг оператора проектирования $\mathbb{R}^3$ на прямую? {
    =1
    =2
    =3
    =0
}

::К3_В5:: Оператор зеркального отражения относительно плоскости в $\mathbb{R}^3$. Является ли он обратимым? {
    =Да, обратен сам себе.
    =Нет, так как меняет ориентацию.
    =Нет, так как определитель равен 0.
    =Только если плоскость проходит через начало координат.
}

::К3_В6:: Что происходит с объемом параллелепипеда при действии необратимого линейного оператора в $\mathbb{R}^3$? {
    =Он превращается в 0 (объем "схлопывается").
    =Он увеличивается в $|\det A|$ раз.
    =Он сохраняется.
    =Он меняется на противоположный по знаку.
}

::К3_В7:: Ядро оператора дифференцирования $\frac{d}{dx}$ в пространстве многочленов степени не выше $n$ — это: {
    =Множество констант (многочлены степени 0).
    =Множество всех многочленов.
    =Множество многочленов степени $n$.
    =Пустое множество.
}

::К3_В8:: Если образ линейного оператора в $\mathbb{R}^3$ является плоскостью, проходящей через начало координат, то его ядро: {
    =Является прямой, проходящей через начало координат.
    =Является плоскостью.
    =Состоит только из нуля.
    =Совпадает со всем пространством.
}

::К3_В9:: Оператор гомотетии (масштабирования) $\hat{H}\vec{x} = k\vec{x}$ в $\mathbb{R}^n$. При каком $k$ оператор необратим? {
    =$k=0$
    =$k=1$
    =$k=-1$
    =При любом $k$.
}

::К3_В10:: Вектор $\vec{b}$ принадлежит образу оператора $\hat{A}$ тогда и только тогда, когда: {
    =Система $A\vec{x}=\vec{b}$ совместна.
    =Система $A\vec{x}=\vec{b}$ имеет единственное решение.
    =Вектор $\vec{b}$ перпендикулярен ядру оператора.
    =$\vec{b}$ является собственным вектором.
}