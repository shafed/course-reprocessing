::К4_В1:: В 3D-графике матрица проекции 3D-точки на 2D-экран необратима. Что это означает физически? {
    =Невозможно однозначно восстановить глубину (Z-координату) точки по её 2D-проекции.
    =Невозможно повернуть камеру обратно.
    =Изображение всегда будет черным.
    =Потеряется цветовая информация.
}

::К4_В2:: В задаче линейной регрессии $AX=Y$ матрица признаков $A$ имеет нетривиальное ядро. Какая проблема возникает? {
    =Мультиколлинеарность: решение неединственно, модель неустойчива.
    =Решение не существует вообще.
    =Модель идеально обучается.
    =Данные линейно независимы.
}

::К4_В3:: В шифре Хилла ключом является матрица $K$. Какое условие необходимо для возможности расшифровки? {
    =$\det K \neq 0$ (и существование обратной по модулю).
    =$\det K = 0$.
    =Матрица $K$ должна быть симметричной.
    =Матрица $K$ должна содержать только единицы.
}

::К4_В4:: Метод PCA (Principal Component Analysis) в Data Science основан на анализе: {
    =Ранга и сингулярных значений матрицы данных (образ оператора).
    =Только определителя матрицы данных.
    =Ядра матрицы данных.
    =Следствия матрицы данных.
}

::К4_В5:: Зачем в геймдеве вычисляют обратную матрицу модели $M^{-1}$? {
    =Для перевода координат из мирового пространства в локальное пространство объекта (или для Ray Tracing).
    =Для ускорения рендеринга за счет удаления вершин.
    =Для изменения цвета текстуры.
    =Для сжатия геометрии.
}

::К4_В6:: Если ранг матрицы изображений (векторизованных кадров) равен 1, что это говорит о видео? {
    =Все кадры пропорциональны друг другу (видео практически статично или меняется только яркость).
    =Видео очень высокого качества.
    =Видео содержит много шума.
    =Каждый кадр уникален.
}

::К4_В7:: В робототехнике, если якобиан манипулятора вырожден (детерминант равен 0), это состояние называется: {
    =Сингулярностью (потеря степеней свободы).
    =Максимальной точностью.
    =Обратимым преобразованием.
    =Линейной независимостью осей.
}

::К4_В8:: При сжатии изображения с потерями (например, JPEG) мы фактически аппроксимируем матрицу изображения матрицей меньшего ранга. Что происходит с ядром оператора сжатия? {
    =Оно становится нетривиальным (часть информации теряется безвозвратно).
    =Оно остается нулевым.
    =Оно совпадает с образом.
    =Ядро не определено для нелинейных операций.
}

::К4_В9:: В компьютерном зрении фундаментальная матрица $F$ связывает соответствующие точки на двух изображениях. Условие $\vec{x}'^T F \vec{x} = 0$. Если $\det F = 0$, то: {
    =Это эпиполярная геометрия, матрица вырождена (ранг 2), что нормально для стереопары.
    =Камеры находятся в одной точке.
    =Изображения идентичны.
    =Ошибка калибровки, матрица должна быть обратима.
}

::К4_В10:: Для чего используется поиск базиса ядра в задачах кодирования с исправлением ошибок? {
    =Для построения проверочной матрицы кода (синдромное декодирование).
    =Для сжатия данных.
    =Для шифрования открытым ключом.
    =Для увеличения скорости передачи.
}