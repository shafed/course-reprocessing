\documentclass[a4paper,12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T2A]{fontenc}
\usepackage[russian]{babel}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage{enumitem}
\usepackage{array}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{titlesec}
\usepackage{setspace}
\usepackage{microtype}
\usepackage{ragged2e}
\usepackage{makecell} % Для явного переноса строк в ячейках таблиц
\usepackage{xcolor}
\usepackage{tcolorbox}

% Настройка полей
\geometry{top=2.5cm, bottom=2cm, left=2cm, right=2cm, headheight=1.5cm, headsep=0.5cm}

% Настройка колонтитулов
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\raisebox{-0.25\height}{\includegraphics[height=1.2cm]{logo.jpg}}\quad\textbf{РТУ МИРЭА, Кафедра ВМ-3}}
\fancyhead[R]{Кейс: Линейные операторы в Computer Vision}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% Настройка заголовков
\titleformat{\section}{\large\bfseries\scshape}{\thesection}{1em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection}{1em}{}

% Оформление для блоков с инструкциями
\definecolor{instrcolor}{RGB}{240, 248, 255}
\newtcolorbox{instructionbox}{
    colback=instrcolor, colframe=blue!50!black, fonttitle=\bfseries,
    title={Инструкция по выполнению}, sharp corners, boxrule=0.8pt
}

\title{\vspace{-2cm}\textbf{Практико-ориентированный кейс по математике}\\[0.5cm]
\large для студентов IT-направлений\\[0.3cm]
\textit{Тема: Линейные операторы.}\\ Ядро, образ и обратимость в задачах обработки изображений}
\author{}
\date{}

\begin{document}

\maketitle
\thispagestyle{fancy}

\begin{abstract}
\noindent\textbf{Аннотация.} Кейс предназначен для формирования у студентов компетенций по анализу линейных преобразований в конечномерных пространствах. На примере задач компьютерного зрения (проекции, аффинные трансформации) студенты учатся вычислять ядро и образ оператора, определять условия обратимости преобразований и интерпретировать геометрический смысл ранга и дефекта матрицы. Работа включает аналитические вычисления в \textbf{Рабочей тетради} и численное моделирование в \textbf{Python}.
\end{abstract}

\tableofcontents
\newpage

\section{Паспорт кейса}
\begin{center}
\renewcommand{\arraystretch}{1.4}
\begin{tabularx}{\textwidth}{@{}lX@{}}
\toprule
\textbf{Параметр} & \textbf{Содержание} \\
\midrule
Дисциплина & Линейная алгебра и геометрия / Математический анализ \\
Направление & Информатика и вычислительная техника, Прикладная математика, Программная инженерия \\
Форма работы & Индивидуальная или парная (2 занятия по 2 ак.~ч.) \\
Цель & Научить студентов применять аппарат линейных операторов для анализа систем уравнений в Computer Vision, оценивать потерю информации при проективных преобразованиях и обосновывать необходимость дополнительных ограничений для восстановления данных \\
Компетенции & Математическое моделирование графических процессов, анализ устойчивости алгоритмов, работа с матричными представлениями операторов, понимание связи между размерностью ядра и неоднозначностью решений \\
\bottomrule
\end{tabularx}
\end{center}

\section{Легенда (Инженерный контекст)}
Вы работаете над модулем дополненной реальности (AR) для мобильного приложения. Система должна отслеживать положение маркера (плоского квадрата) в пространстве и проецировать на него 3D-объекты. 

Камера смартфона фиксирует 2D-изображение. Математически процесс получения кадра описывается линейным оператором проекции $\hat{P}$, переводящим векторы из трехмерного мирового пространства $V_3$ в двумерное пространство изображения $V_2$ (в однородных координатах это линейный оператор в расширенном смысле, но на этапе геометрического анализа мы рассматриваем ортогональную проекцию на плоскость сенсора).

Однако возникает проблема: по одному 2D-кадру невозможно однозначно определить расстояние до объекта (глубину). Более того, при попытке восстановить исходные координаты маркера по его искаженной проекции возникает система линейных уравнений.

\medskip
\noindent\textbf{Разработчику алгоритма необходимо:}
\begin{enumerate}[label=\arabic*.]
    \item Описать оператор проекции и найти его ядро, чтобы понять, какие точки пространства «неразличимы» для камеры.
    \item Проанализировать оператор аффинного искажения (поворот + масштаб), применяемый к маркеру, и проверить его обратимость.
    \item Выяснить, почему при определенных углах обзора (вырожденных случаях) система становится неустойчивой (ранг матрицы падает).
    \item Обосновать, сколько независимых параметров можно восстановить из одного кадра, используя теорему о ранге и дефекте.
\end{enumerate}

\section{Исходные данные и материалы}
Для выполнения кейса вам потребуются следующие файлы (выдаются преподавателем или скачиваются из LMS):
\begin{itemize}[label=$\bullet$]
    \item \texttt{Worksheet\_Operators.tex} -- \textbf{Рабочая тетрадь}. Заполните её от руки или в редакторе LaTeX. Сюда вносятся все аналитические выкладки, доказательства и промежуточные ответы.
    \item \texttt{lab\_operators.py} -- \textbf{Шаблон кода на Python}. Используйте его для численной проверки ваших расчетов, визуализации луча зрения и анализа вырожденных случаев.
\end{itemize}

\medskip
\noindent\textbf{Математическая модель:}
\begin{itemize}[label=$\bullet$]
    \item Мировое пространство объектов описывается декартовыми координатами $(x, y, z)$ в базисе $\mathcal{E} = \{\vec{i}, \vec{j}, \vec{k}\}$.
    \item Плоскость изображения совпадает с плоскостью $Oxy$ ($z=0$).
    \item Оператор ортогональной проекции $\hat{P}: V_3 \to V_3$ действует по правилу: $\hat{P}(x, y, z) = (x, y, 0)$.
    \item Маркер в своем локальном базисе представляет собой единичный квадрат. Его положение в мире задается вектором смещения $\vec{t}$ и матрицей поворота $R$.
    \item Для анализа обратимости рассмотрим оператор $\hat{A}$, описывающий трансформацию координат маркера при его движении в плоскости, параллельной экрану. Матрица оператора в некотором базисе имеет вид:
    \[
    A = \begin{pmatrix} 
    2 & -1 & 0 \\ 
    1 & 0 & 0 \\ 
    0 & 0 & k 
    \end{pmatrix},
    \]
    где $k$ -- параметр, зависящий от удаления объекта от камеры (масштабный коэффициент перспективы, аппроксимированный линейно для малых углов).
\end{itemize}

\section{Задания для студентов}

\begin{instructionbox}
\textbf{Важно:} Выполняйте задания последовательно. Сначала делайте аналитические расчеты в \textbf{Рабочей тетради}, затем подтверждайте их результатами из \textbf{Python-скрипта}. В тетради указывайте выводы, полученные из кода (например, ``Совпадает с выводом numpy.linalg.inv'').
\end{instructionbox}

\begin{enumerate}[label=\textbf{\arabic*.}, leftmargin=*]
    \item \textbf{Геометрический анализ проекции (Аналитика + Код).}
    \begin{enumerate}[label=\alph*), leftmargin=2em]
        \item Запишите матрицу оператора $\hat{P}$ в стандартном базисе в разделе 1 Рабочей тетради.
        \item Найдите ядро $\text{Ker }\hat{P}$ и образ $\text{Im }\hat{P}$ аналитически (решением системы линейных уравнений). Запишите базисные векторы в тетрадь.
        \item \textit{Проверка в Python:} Запустите раздел 1 скрипта \texttt{lab\_operators.py}. Сравните базис ядра, полученный функцией \texttt{scipy.linalg.null\_space}, с вашим ручным расчетом. Впишите в тетрадь размерности ядра и образа, выведенные программой.
        \item Дайте геометрическую интерпретацию ядра в контексте работы камеры (что такое «луч зрения»?).
    \end{enumerate}

    \item \textbf{Анализ обратимости трансформации маркера (Аналитика + Код).}
    \begin{enumerate}[label=\alph*), leftmargin=2em]
        \item Вычислите определитель матрицы $A$ аналитически (разложением по строке/столбцу) как функцию от $k$. Запишите условие обратимости в тетрадь.
        \item Найдите матрицу обратного оператора $\hat{A}^{-1}$ для случая $k=1$ вручную (методом Гаусса или через алгебраические дополнения).
        \item \textit{Проверка в Python:} В разделе 2 скрипта установите \texttt{k=1.0}. Сравните вашу матрицу $A^{-1}$ с выводом \texttt{numpy.linalg.inv}. Вставьте распечатку или скриншот результата в отчет (или перепишите в тетрадь с пометкой ``Проверено кодом'').
        \item Исследуйте предельный случай $k \to 0$. Что происходит с ядром оператора? Почему восстановление положения маркера становится невозможным? Проиллюстрируйте это, запустив код с \texttt{k=1e-9}.
    \end{enumerate}

    \item \textbf{Задача восстановления координат (Системы линейных уравнений).}
    Пусть мы наблюдаем проекцию точки $\vec{y} = (2, 1, 0)^T$.
    \begin{enumerate}[label=\alph*), leftmargin=2em]
        \item Запишите общее решение уравнения $\hat{P}\vec{x} = \vec{y}$ в виде $\vec{x} = \vec{x}_0 + C\vec{v}$. Заполните соответствующий раздел в Рабочей тетради.
        \item Покажите, что решение неединственно. Какое дополнительное ограничение (регуляризация) нужно наложить на $\vec{x}$, чтобы выбрать единственную точку (например, лежащую на известной плоскости $z=5$)?
        \item \textit{Расчет в Python:} Используйте раздел 3 скрипта для нахождения частного решения через псевдообратную матрицу (\texttt{numpy.linalg.pinv}) и построения итогового вектора при условии $z=5$.
    \end{enumerate}

    \item \textbf{IT-приложение: Сжатие данных и PCA (Теоретический вопрос).}
    Предположим, что облако точек, описывающее объект, лежит в подпространстве меньшей размерности. Если матрица данных $M$ имеет ранг $r < n$, объясните в разделе 4 Рабочей тетради, как понятия ядра и образа помогают в методе главных компонент (PCA) для сжатия информации без существенной потери качества.
\end{enumerate}

\section{Методические рекомендации}
\begin{itemize}[label=$\triangleright$]
    \item Помните, что ядро оператора -- это множество всех векторов, которые отображаются в ноль. В контексте проекции это векторы, «схлопывающиеся» в точку начала координат экрана.
    \item Для проверки обратимости используйте критерий: оператор обратим $\iff \det A \neq 0 \iff \text{Ker } A = \{\vec{0}\}$.
    \item Общее решение неоднородного уравнения $A\vec{x}=\vec{y}$ есть сумма частного решения $\vec{x}_0$ и общего решения однородного уравнения $\vec{x}_h \in \text{Ker } A$.
    \item При анализе параметра $k$ рассмотрите случаи $k=0$ и $k \neq 0$ отдельно.
    \item \textbf{Внимание:} Функция \texttt{null\_space} в SciPy возвращает нормированные векторы. Ваш ручной базис может отличаться множителем -- это нормально, главное, чтобы направления совпадали.
\end{itemize}

\section{Подробное решение (ключевые этапы)}

\subsection{Геометрический анализ проекции}
Матрица оператора ортогональной проекции на плоскость $Oxy$:
\[
P = \begin{pmatrix} 
1 & 0 & 0 \\ 
0 & 1 & 0 \\ 
0 & 0 & 0 
\end{pmatrix}.
\]

\textbf{Ядро:} Решаем систему $P\vec{x} = \vec{0}$:
\[
\begin{pmatrix} 1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 0 \end{pmatrix} \begin{pmatrix} x \\ y \\ z \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \\ 0 \end{pmatrix} \implies \begin{cases} x=0 \\ y=0 \\ z \in \mathbb{R} \end{cases}.
\]
$\text{Ker }\hat{P} = \text{span}(\vec{k}) = \{(0,0,z)^T \mid z \in \mathbb{R}\}$. 
\textit{Геометрический смысл:} Это ось $OZ$. Все точки, лежащие на перпендикуляре к экрану, проходящем через центр объектива, проецируются в одну точку (центр экрана). Для камеры эти точки неразличимы по положению на сенсоре.

\textbf{Образ:} Столбцы матрицы $P$ порождают образ. Первые два столбцы линейно независимы, третий нулевой.
$\text{Im }\hat{P} = \text{span}(\vec{i}, \vec{j}) = \{(x,y,0)^T \mid x,y \in \mathbb{R}\}$.
Это сама плоскость экрана $Oxy$.

\textbf{Размерности:}
$\dim(\text{Ker }\hat{P}) = 1$ (дефект).
$\dim(\text{Im }\hat{P}) = 2$ (ранг).
$\dim V_3 = 3$.
Теорема: $1 + 2 = 3$. Верно.

\subsection{Анализ обратимости трансформации маркера}
Матрица $A = \begin{pmatrix} 2 & -1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & k \end{pmatrix}$.

\textbf{Условие обратимости:}
Вычислим определитель, разложив по третьей строке:
\[
\det A = k \cdot (-1)^{3+3} \cdot \det \begin{pmatrix} 2 & -1 \\ 1 & 0 \end{pmatrix} = k \cdot (0 - (-1)) = k.
\]
Оператор $\hat{A}$ обратим тогда и только тогда, когда $\det A \neq 0$, т.е. \textbf{$k \neq 0$}.

\textit{Через ядро:} Если $k \neq 0$, то система $A\vec{x}=\vec{0}$ имеет только тривиальное решение $\vec{x}=\vec{0}$ (так как первые две строки дают $x=y=0$, а третья $kz=0 \implies z=0$). Ядро тривиально $\implies$ оператор инъективен $\implies$ обратим.

\textbf{Обратный оператор при $k=1$:}
\[
A = \begin{pmatrix} 2 & -1 & 0 \\ 1 & 0 & 0 \\ 0 & 0 & 1 \end{pmatrix}.
\]
Найдем $A^{-1}$ методом Гаусса или через алгебраические дополнения.
Для блока $2 \times 2$: $\begin{pmatrix} 2 & -1 \\ 1 & 0 \end{pmatrix}^{-1} = \frac{1}{1} \begin{pmatrix} 0 & 1 \\ -1 & 2 \end{pmatrix} = \begin{pmatrix} 0 & 1 \\ -1 & 2 \end{pmatrix}$.
Тогда:
\[
A^{-1} = \begin{pmatrix} 0 & 1 & 0 \\ -1 & 2 & 0 \\ 0 & 0 & 1 \end{pmatrix}.
\]
\textit{Физический смысл:} Умножение координат проекции на $A^{-1}$ позволяет восстановить исходные локальные координаты маркера (с точностью до масштаба и поворота), что необходимо для корректного наложения 3D-модели.

\textbf{Предельный переход $k \to 0$:}
Если $k=0$, то $\det A = 0$. Оператор вырожден.
Ядро: $2x-y=0, x=0 \implies y=0$, $0z=0 \implies z \in \mathbb{R}$.
$\text{Ker } A = \text{span}(\vec{k})$.
Это означает, что любая точка вида $(0,0,z)$ переходит в нуль. Информация о координате $z$ (глубине) полностью теряется в этой модели. Восстановление становится невозможным.

\subsection{Задача восстановления}
Уравнение $\hat{P}\vec{x} = \vec{y}$, где $\vec{y}=(2,1,0)^T$.
\[
\begin{pmatrix} 1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 0 \end{pmatrix} \begin{pmatrix} x \\ y \\ z \end{pmatrix} = \begin{pmatrix} 2 \\ 1 \\ 0 \end{pmatrix} \implies \begin{cases} x=2 \\ y=1 \\ 0=0 \end{cases}.
\]
Переменная $z$ может быть любой.
Общее решение: $\vec{x} = \begin{pmatrix} 2 \\ 1 \\ 0 \end{pmatrix} + z \begin{pmatrix} 0 \\ 0 \\ 1 \end{pmatrix}, \quad z \in \mathbb{R}$.

\textit{Интерпретация:} Мы видим точку на экране в позиции $(2,1)$, но не знаем, насколько далеко она находится. Это луч, исходящий из камеры.
\textit{Регуляризация:} Если мы знаем, что маркер лежит на столе, описываемом плоскостью $z=5$, то мы накладываем условие $z=5$. Решение становится единственным: $\vec{x} = (2, 1, 5)^T$. В AR-системах это делается с помощью датчика глубины (LiDAR) или стереопары (дополнительных уравнений).

\subsection{IT-приложение: Сжатие данных и PCA}
В методе главных компонент (PCA) мы работаем с матрицей данных $X$. Если $\text{rank}(X) = r \ll n$, это означает, что все векторы данных лежат в $r$-мерном подпространстве (образе оператора низкой размерности).
\begin{itemize}
    \item \textbf{Ядро} ковариационной матрицы соответствует направлениям с нулевой дисперсией (шум или избыточность).
    \item Проецируя данные на \textbf{образ} (базис из $r$ главных компонент), мы отбрасываем компоненты, лежащие вблизи ядра шумового оператора.
    \item Это позволяет сжать изображение или признак, храня только коэффициенты разложения по базису образа, экономя память и ускоряя обучение нейросетей.
\end{itemize}

\section{Инженерная интерпретация и выводы}
\begin{enumerate}[label=\arabic*.]
    \item \textbf{Необратимость проекции:} Ортогональная проекция -- классический пример необратимого оператора. Потеря одной координаты (глубины) означает, что ядро оператора нетривиально. Однозначное восстановление 3D по 2D невозможно без априорной информации (ограничений).
    \item \textbf{Роль определителя:} В алгоритмах трекинга маркеров необходимо следить, чтобы матрица гомографии (или аффинного приближения) оставалась невырожденной. Если определитель стремится к нулю (маркер выродился в линию или точку на экране), алгоритм теряет устойчивость, и ошибки округления приводят к огромным погрешностям в восстановлении координат.
    \item \textbf{Размерность образа:} Ранг матрицы камеры определяет количество степеней свободы, которые мы можем измерить. Для монокулярной камеры ранг проекции равен 2, поэтому мы теряем 1 степень свободы. Стереокамера (две проекции) повышает ранг системы уравнений, позволяя восстановить 3 степени свободы (при правильной базе).
    \item \textbf{Связь с ML:} Понимание ядра и образа критично для отладки нейросетей. Если входные признаки линейно зависимы (попадают в ядро весовой матрицы следующего слоя), часть нейронов «умирает» (не обучается), так как градиент по этим направлениям равен нулю.
\end{enumerate}

\section{Критерии оценки}
\renewcommand{\arraystretch}{1.25}
\footnotesize
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{0.35\textwidth}c>{\raggedright\arraybackslash}X@{}}
\toprule
\textbf{Компонент} & \textbf{Баллы} & \textbf{Требования} \\
\midrule
Рабочая тетрадь (Аналитика) & 5 & \makecell[l]{Верно найдены базисы ядра и образа, \\ заполнены все пропуски в тетради, \\ дана корректная геометрическая интерпретация} \\
Python-лаборатория (Код) & 3 & \makecell[l]{Код запущен без ошибок, результаты сверены с рукой, \\ вставлены скриншоты/выводы в отчет или тетрадь} \\
Анализ обратимости и систем & 3 & \makecell[l]{Верно использован критерий через определитель и ядро, \\ найдена обратная матрица, проанализирован случай $k=0$, \\ верно записано общее решение системы} \\
IT-интерпретация и выводы & 2 & \makecell[l]{Глубокое понимание связи ранга/ядра с проблемами \\ восстановления 3D, устойчивостью алгоритмов и PCA} \\
Оформление и защита & 2 & \makecell[l]{Чёткая структура, аккуратные математические записи, \\ логичные выводы, успешные ответы на вопросы} \\
\midrule
\multicolumn{2}{@{}l}{\textbf{Итого}} & \textbf{15} \\
\bottomrule
\end{tabularx}

\medskip
\noindent\textbf{Шкала оценивания:}
\begin{itemize}[label=$\circ$, leftmargin=*]
    \item \textbf{13--15 баллов:} Отлично. Безупречное владение аппаратом, глубокая связь с компьютерной графикой и ML, полное заполнение тетради и кода.
    \item \textbf{10--12 баллов:} Хорошо. Верные расчёты, но поверхностная интерпретация физического смысла ядра/образа, мелкие недочеты в коде.
    \item \textbf{7--9 баллов:} Удовлетворительно. Есть ошибки в нахождении обратной матрицы или базиса ядра, слабые выводы, код не сверен с рукой.
    \item \textbf{$<7$ баллов:} Неудовлетворительно. Не понято различие между ядром и образом, неверно применён критерий обратимости, тетрадь пуста.
\end{itemize}

\section{Вопросы для защиты / дискуссии}
\begin{enumerate}[label=\textbf{\alph*)}, leftmargin=*, itemsep=0.5em]
    \item Почему в компьютерной графике используют однородные координаты (4 компонента для 3D)? Как это превращает аффинные преобразования (сдвиг) в линейные операторы и влияет на обратимость?
    \item Может ли линейный оператор из $\mathbb{R}^3$ в $\mathbb{R}^2$ быть обратимым? Почему? (Ответ: нет, так как размерности пространств различны, нарушается условие биективности).
    \item Как эффект «Гимбал Лок» (Gimbal Lock) в анимации связан с потерей ранга матрицы вращения при использовании углов Эйлера?
    \item В задаче рекомендательных систем матрица «пользователь-товар» обычно разреженная и неполная. Как методы понижения ранга (SVD) помогают заполнить пропуски, опираясь на понятие образа оператора?
    \item Если матрица весов полносвязного слоя нейросети имеет большое ядро, что это говорит о её эффективности? Можно ли удалить некоторые нейроны без потери качества?
\end{enumerate}

\end{document}